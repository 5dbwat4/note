---
title: XXE
---

# 将修改后的docx重新打包成zip文件

```shell
zip -0X new1.docx "[Content_Types].xml"
zip -r -9 new1.docx * -x "[Content_Types].xml" -n ".png:.jpg:.jpeg:.gif"
```

大致说法是：`[Content_Types].xml`要放在压缩包的开头，然后压缩要用deflate格式