9. 下面C++代码的运行结果为 ()

```cpp
#include <iostream>
#include <utility>

using namespace std;

void modify(int &&rref) {
    rref = 100;
    cout << rref << ",";
}

int main() {
    int num = 50;
    modify(move(num));
    cout << num;
    return 0;
}
```



3.单选 移动端网络从 Wi-Fi 切换到 4G 时，由于终端 IP 发生变化，原有 TCP 连接通常会中断。若希望在不断线的前提下继续同一逻辑会话，更常见的做法是 ()

A 依赖 TCP 自带的连接迁移功能自动保持原四元组不变  
B 依赖 DNS 将域名解析为旧 IP 从而维持原 TCP 连接不断线  
C 依赖 HTTP 重定向让旧连接继续传输并自动修复中断  
D 应用层发起重连，并通过 Token 在新连接上恢复会话状态  



4. 在两级页表中，逻辑地址被划分为页目录项索引p1、页表项索引p2与页内偏移d。若页大小为4KB且每个页表项占4B，则p2字段可索引的页表项个数为 ()

A 512  
B 1024  
C 2048  
D 256  



5. 下列C++代码的输出结果是什么 ()

```cpp
#include <iostream>
using namespace std;

template<typename T>
void print(T &&t) { cout << "universal" << endl; }

void print(int &x) { cout << "lvalue" << endl; }

int main() {
    int x = 5;
    print(x);
    print(5);
}
```



7. 关于C++中dynamic_cast和typeid运算符的正确说法是 ()

A dynamic_cast进行向下转型时，要求被转换的指针或引用的静态类型为多态类型（即包含虚函数）  
B 对空指针使用typeid运算符，无论是解引用形式还是直接作用于指针，都会抛出bad_typeid异常  
C dynamic_cast在进行向下转型时，可以完全不需要虚函数表的运行时支持  
D typeid运算符作用于任何表达式时，都能准确返回该对象的实际动态类型信息  



不定项（2分） ● 错选不得分，少选得1/3分



1. 在C++中，关于指针的基本操作和概念，以下哪些说法是正确的 ()

A 可以将一个nullptr赋值给任意类型的指针变量以表示其未指向任何对象  
B 指针变量在未初始化时，直接解引用可能会导致不可预期的程序崩溃  
C 指针变量只能存储栈上变量的地址，不能存储堆上分配对象的地址  
D 指针类型必须与其所指向变量的类型保持一致，否则可能导致未定义行为  

4. 关于C++中的函数重载和函数模板特化的关系，以下说法正确的有哪些 ()

A 函数模板的全特化版本参与重载决议时，编译器首先根据主模板进行重载决议，选定主模板后再考虑是否有匹配的特化版本  
B 函数模板不支持偏特化，只能进行全特化，如果需要实现类似偏特化的效果，可以使用函数重载或SFINAE技术  
C 两个函数模板如果只有返回值类型不同，参数列表完全相同，编译器会根据调用处期望的返回类型自动选择合适的模板版本  
D 当函数调用既匹配普通函数又匹配函数模板时，在参数类型完全匹配的情况下，编译器优先选择普通函数而不是模板实例化版本  



5. 关于TCP拥塞控制在丢包场景下的行为，正确的是

A 不同算法如CUBIC与BBR在带宽探测方式上有差异  
B 丢包不一定等价于拥塞也可能是无线误码造成  
C 只要启用SACK就不会再触发拥塞窗口下降行为  
D 可能进入快速恢复并调整ssthresh与cwnd  



11. 在C++中关于友元函数和友元类的特性和使用，以下描述正确的有哪些 ()

A 友元关系不具有传递性，如果类ClassA是类ClassB的友元，类ClassB是类ClassC的友元，这并不意味着类ClassA是类ClassC的友元  
B 友元关系不具有对称性，如果类ClassA声明类ClassB为友元，类ClassB可以访问ClassA的私有成员，但反过来ClassA不能访问ClassB的私有成员  
C 派生类会自动继承基类的友元关系，基类的友元函数也是派生类的友元函数  
D 友元函数虽然可以访问类的私有成员和保护成员，但它不是类的成员函数，不能通过对象使用成员访问运算符来调用  



10. 关于C++中const关键字和constexpr关键字的区别与使用场景，以下说法正确的有哪些 ()

A constexpr函数在C++11中只能包含单个return语句，而C++14放宽了这个限制，允许包含局部变量声明和循环等控制语句  
B const成员函数承诺不修改对象的任何成员变量，但可以通过mutable关键字标记的成员变量例外  
C 在类外定义的const全局变量默认具有外部链接性，与普通全局变量的链接性相同  
D constexpr变量必须在编译期就能确定其值，而const变量可以在运行时初始化，只是初始化后不能修改  


14. 关于C++中的模板特化，下列说法正确的有 ()

A 类模板可以进行全特化和部分特化  
B 使用模板特化可以为特定类型提供更高效的实现  
C 显式特化必须在主模板所在的命名空间作用域中声明  
D 函数模板可以进行全特化，也可以进行部分特化  



## 2



给定四个整数 $ n, k, m, r $，其中  
$ 0 \le r \le m - 1 $ 且 $ m \ge 1 $。判断是否可以将 $ n $ 表示为 $ k $ 个两两不同的正整数之和，且每个数都与 $ r $ 在模 $ m $ 意义下同余。若可行，输出一组构造；否则输出 NO。

$$
\exists a_1, \dots, a_k \in \mathbb{Z}_{>0} \text{ s.t. } \sum_{i=1}^{k} a_i = n,\ a_i \equiv r \pmod{m},\ a_i \text{ 两两不同}
$$

---

### 输入描述

每个测试文件均包含多组测试数据。第一行输入一个整数 $ t \ (1 \le t \le 10^5) $ 表示测试用例数量。  
对每个测试用例，输入一行四个整数...

---

对于每个测试用例：

- 若不存在表示，输出一行 `NO`；
- 否则输出一行 `YES`，并在下一行输出 $ k $ 个两两不同的正整数 $ a_1, \dots, a_k $（顺序任意，需满足同余与求和约束）。若存在多个合法答案，输出任意一组即可。

---

#### 示例 1

**输入**

```
3
15 3 4 1
18 3 5 3
36 3 6 0
```

**输出**

```
YES
1 5 9
NO
YES
6 12 18
```

**说明**



## 3