---
title: Chapter 3 Multivariate Random Variables and Their Distributions
---

# **§3.1 二维随机变量**

## 1. 定义
- **二维随机变量**：$(X, Y)$，定义在同一样本空间上的两个随机变量。
- **二维离散型随机变量**：$(X, Y)$ 取值有限或可列对。
- **联合分布律**：
  $$
  P(X = x_i, Y = y_j) = p_{ij}
  $$
  满足：
  - $p_{ij} \geq 0$
  - $\sum_i \sum_j p_{ij} = 1$

## 2. 边际分布律
- $P(X = x_i) = \sum_j p_{ij} = p_{i\cdot}$
- $P(Y = y_j) = \sum_i p_{ij} = p_{\cdot j}$

## 3. 条件分布律
- 给定 $Y = y_j$：
  $$
  P(X = x_i | Y = y_j) = \frac{p_{ij}}{p_{\cdot j}}
  $$
- 给定 $X = x_i$：
  $$
  P(Y = y_j | X = x_i) = \frac{p_{ij}}{p_{i\cdot}}
  $$

---

# **§3.2 分布函数**

## 1. 联合分布函数
$$
F(x, y) = P(X \leq x, Y \leq y)
$$

## 2. 性质
- 单调不减、右连续
- $F(-\infty, y) = F(x, -\infty) = 0$，$F(+\infty, +\infty) = 1$
- 对 $x_1 < x_2$，$y_1 < y_2$：
  $$
  P(x_1 < X \leq x_2, y_1 < Y \leq y_2) = F(x_2, y_2) - F(x_2, y_1) - F(x_1, y_2) + F(x_1, y_1)
  $$

## 3. 边际分布函数
- $F_X(x) = F(x, +\infty)$
- $F_Y(y) = F(+\infty, y)$

## 4. 条件分布函数
- 若 $P(Y = y) > 0$：
  $$
  F_{X|Y}(x|y) = \frac{P(X \leq x, Y = y)}{P(Y = y)}
  $$
- 若 $P(Y = y) = 0$：
  $$
  F_{X|Y}(x|y) = \lim_{y' \to y} \frac{P(X \leq x, y < Y \leq y')}{P(y < Y \leq y')} \text{仍记为} P(X \leq x, Y = y)
  $$
---

# **§3.3 二维连续型随机变量**

## 1. 联合概率密度函数
$$
F(x, y) = \int_{-\infty}^x \int_{-\infty}^y f(u, v) du dv
$$

## 2. 性质
- $f(x, y) \geq 0$
- $\int_{-\infty}^{+\infty} \int_{-\infty}^{+\infty} f(x, y) dx dy = 1$
- $P((X, Y) \in G) = \iint_G f(x, y) dx dy$
- $\frac{\partial^2 F}{\partial x \partial y} = f(x, y)$（连续点）

## 3. 边际概率密度
- $f_X(x) = \int_{-\infty}^{+\infty} f(x, y) dy$
- $f_Y(y) = \int_{-\infty}^{+\infty} f(x, y) dx$

## 4. 条件概率密度
- 给定 $Y = y$：
  $$
  f_{X|Y}(x|y) = \frac{f(x, y)}{f_Y(y)}
  $$
- 给定 $X = x$：
  $$
  f_{Y|X}(y|x) = \frac{f(x, y)}{f_X(x)}
  $$

（适用于 *求$P\{Y > 1|X = 1\}$* 这种问题，因为你肯定没法直接$\int^{0.5}_{0.5} \cdots$）

## 5. 二维均匀分布
- 若 $(X, Y)$ 在区域 $D$ 上均匀分布：
  $$
  f(x, y) = \frac{1}{S_D}, \quad (x, y) \in D
  $$
  - $P((X, Y) \in D_1) = \frac{S_{D_1}}{S_D}$


## 二维正态分布
$$
(X,Y) \sim N(\mu_1, \mu_2; \sigma_1^2, \sigma_2^2; \rho)
$$
- 边际分布：$X \sim N(\mu_1, \sigma_1^2)$，$Y \sim N(\mu_2, \sigma_2^2)$
- 条件分布：
  - $Y|X=x \sim N\left(\mu_2 + \rho \frac{\sigma_2}{\sigma_1}(x - \mu_1), (1-\rho^2)\sigma_2^2\right)$

---

# §3.4 随机变量的独立性

## 定义
$$
F(x,y) = F_X(x) F_Y(y) \quad \text{或} \quad f(x,y) = f_X(x) f_Y(y)
$$

## 离散型
$$
p_{ij} = p_{i\cdot} p_{\cdot j}
$$

## 连续型
$$
f(x,y) = f_X(x) f_Y(y) \quad \text{几乎处处成立}
$$

## 判定定理（连续型）
$$
f(x,y) = m(x) \cdot n(y) \Rightarrow X,Y \text{ 独立}
$$

## 二维正态独立 ⇔ $\rho = 0$

---

# §3.5 多维随机变量函数的分布

## 离散型：$Z = g(X,Y)$
- 列出 $Z$ 的可能取值，合并概率。

## 连续型：$Z = X + Y$
$$
f_Z(z) = \int_{-\infty}^{+\infty} f(x, z-x) dx
$$
若 $X,Y$ 独立：
$$
f_Z(z) = \int_{-\infty}^{+\infty} f_X(x) f_Y(z-x) dx \quad \text{（卷积公式）}
$$

## 极值分布（独立）
- $M = \max(X,Y)$：
  $$
  F_M(z) = F_X(z) F_Y(z)
  $$
- $N = \min(X,Y)$：
  $$
  F_N(z) = 1 - [1 - F_X(z)][1 - F_Y(z)]
  $$

## 推广到 $n$ 个独立同分布
- $M = \max(X_1,...,X_n)$：$F_M(z) = [F(z)]^n$
- $N = \min(X_1,...,X_n)$：$F_N(z) = 1 - [1 - F(z)]^n$

# 🔢 典型结论速查

- **独立正态和**：
  $$
  X+Y \sim N(\mu_1+\mu_2, \sigma_1^2+\sigma_2^2)
  $$
- **独立泊松和**：
  $$
  X+Y \sim P(\lambda_1 + \lambda_2)
  $$
- **独立均匀和**（$U(0,1)$）：
  $$
  f_Z(z) = 
  \begin{cases}
  z, & 0 < z < 1 \\
  2 - z, & 1 \leq z < 2 \\
  0, & \text{其他}
  \end{cases}
  $$
- **指数分布极值**：
  - 串联：$Z = \min(X,Y) \sim \text{Exp}(\alpha + \beta)$
  - 并联：$Z = \max(X,Y)$ 密度为 $\alpha e^{-\alpha z} + \beta e^{-\beta z} - (\alpha+\beta)e^{-(\alpha+\beta)z}$

# 使用建议

- 熟记**联合、边际、条件**三者关系图。
- 判断独立性：先看定义域是否可分离，再看函数是否可拆。
- 卷积公式仅适用于**独立**连续变量。
- 极值分布公式适用于**独立**变量。
