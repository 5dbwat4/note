

## §5.1 大数定律

### 1. 依概率收敛
**定义**：  
若  
$$
\lim_{n \to \infty} P(|Y_n - Y| \geq \varepsilon) = 0 \quad \forall \varepsilon > 0
$$  
则称 $ Y_n \xrightarrow{P} Y $。

---

### 2. Markov 不等式
$$
P(|Y| \geq \varepsilon) \leq \frac{E(|Y|^k)}{\varepsilon^k}, \quad k \geq 1
$$

---

### 3. Chebyshev 不等式
$$
P(|X - \mu| \geq \varepsilon) \leq \frac{\sigma^2}{\varepsilon^2}
$$  
等价形式：  
$$
P(|X - \mu| < \varepsilon) \geq 1 - \frac{\sigma^2}{\varepsilon^2}
$$

---

### 4. 大数定律（弱大数定律）

**Bernoulli 大数定律**：  
$$
\frac{n_A}{n} \xrightarrow{P} p
$$

**Khinchin 大数定律**：  
若 $X_i$ 独立同分布，且 $E(X_i) = \mu$，则  
$$
\frac{1}{n} \sum_{i=1}^n X_i \xrightarrow{P} \mu
$$

**Chebyshev 大数定律**：  
若 $X_i$ 独立，方差有共同上界，则  
$$
\frac{1}{n} \sum_{i=1}^n X_i - \frac{1}{n} \sum_{i=1}^n E(X_i) \xrightarrow{P} 0
$$

---

## §5.2 中心极限定理

### 1. Lindeberg–Lévy CLT（独立同分布）
若 $X_i$ 独立同分布，$E(X_i) = \mu$，$Var(X_i) = \sigma^2$，则当 $n$ 充分大时：
$$
\sum_{i=1}^n X_i \stackrel{\text{近似}}{\sim} N(n\mu, n\sigma^2)
$$  
$$
\bar{X} \stackrel{\text{近似}}{\sim} N\left(\mu, \frac{\sigma^2}{n}\right)
$$

---

### 2. De Moivre–Laplace CLT（二项分布近似）
若 $n_A \sim B(n, p)$，则当 $n$ 充分大时：
$$
n_A \stackrel{\text{近似}}{\sim} N(np, np(1-p))
$$  
标准化：
$$
\frac{n_A - np}{\sqrt{np(1-p)}} \stackrel{\text{近似}}{\sim} N(0,1)
$$

## 🧠 记忆要点

- **依概率收敛**：概率趋近于 1 时，随机变量趋近于某个值。
- **Chebyshev 不等式**：用于估计概率上界，适用于任何分布。
- **大数定律**：样本均值趋近于总体均值。
- **中心极限定理**：样本和或均值近似服从正态分布。
- **二项分布近似**：可用正态分布近似，尤其当 $n$ 大时。

